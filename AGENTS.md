# AGENTS.md

## Cursor Cloud specific instructions

This repository is currently an empty/placeholder project with only a `README.md` file. There are no applications, services, dependencies, build systems, linters, or test frameworks configured.

- **No package manager lockfiles** exist (`package-lock.json`, `yarn.lock`, `pnpm-lock.yaml`, `requirements.txt`, etc.).
- **No build or run scripts** are present.
- **No Docker or container configuration** exists.
- **No CI/CD pipelines** are configured.

When the repository gains actual source code and dependencies, this section should be updated with:
- How to install dependencies
- How to run lint, tests, and build
- How to start the development server(s)
- Any non-obvious caveats discovered during setup
